# dumby
A contract‑driven, AI‑safe static UI builder.  dumby is a deliberately strict tool for generating static frontend scaffolds from API definitions, while making the project safe for both humans and AI to work on.  It does not try to be smart. It tries to be correct, boring, and controllable.
